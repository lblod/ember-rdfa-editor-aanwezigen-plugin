{{#power-select
   loadingMessage="Aan het laden..."
   noMatchesMessage="Geen resultaten"
   searchMessage="Typ om te zoeken"
   renderInPlace=true
   placeholder="Zoek op achternaam"
   allowClear=allowClear
   search=(perform searchByName)
   selected=_mandataris
   onchange=(action "select")
   as |mandataris|}}
  {{mandataris.isBestuurlijkeAliasVan.gebruikteVoornaam}} {{mandataris.isBestuurlijkeAliasVan.achternaam}},
  {{mandataris.bekleedt.bestuursfunctie.label}}
  {{moment-format mandataris.isBestuurlijkeAliasVan.geboorte.datum 'DD/MM/YYYY'}}
{{/power-select}}
